# RusTunnel — client configuration
ws_url     = "wss://rp.hermesbackend.xyz/tunnel"
auth_token = "change-me-secret"

[[tunnel]]
name        = "minecraft"
remote_port = 25565
local_port  = 25565
protocol    = "tcp"

[[tunnel]]
name        = "api"
remote_port = 8080
local_port  = 3000
protocol    = "tcp"
